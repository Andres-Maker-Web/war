import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'war_20212030',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
